# Copier vers .env puis redémarrer : npm run dev
VITE_API_URL=http://localhost:3000

# Couverture terrain : Leaflet — satellite (Esri World Imagery) par défaut dans l’UI, plan (OSM) au choix. Pas de clé API.
# OSM (plan) : usage intensif → https://operations.osmfoundation.org/policies/tiles/
# Satellite : conditions d’usage Esri / fournisseur à respecter en production.
